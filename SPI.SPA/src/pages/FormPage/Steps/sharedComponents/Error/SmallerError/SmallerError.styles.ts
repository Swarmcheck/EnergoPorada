import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const SmallerErrorBoxBoxStyled = styled(Box)(({
  display: 'flex',
  flexDirection: 'row',
  alignContent: 'center',
  marginTop: '8.5px',
}));