import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const FAQPageBoxStyled = styled(Box)(({
  marginLeft: '200px',
  marginRight: '197px',
  marginTop: '65px',
  marginBottom: '5%',
}));

export const FAQListBoxStyled = styled(Box)(({
  marginTop: '86px',
  marginBottom: '57px'
}))